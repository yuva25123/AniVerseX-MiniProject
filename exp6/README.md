# Exp6

Node.js and Express multi-tier backend for AniVerseX.

## Files

- `package.json`
- `package-lock.json`
- `server.js`
- `models/`
- `routes/`

## Run

1. Run `npm install`
2. Run `npm run dev`
