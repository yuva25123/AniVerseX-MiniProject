# Exp5

Spring Boot MVC CRUD with MySQL and JSP pages.

## Files

- `pom.xml`
- `database/aniversex.sql`
- `src/`

## Run

1. Create the MySQL database using `database/aniversex.sql`
2. Update DB credentials in `src/main/resources/application.properties`
3. Run the Spring Boot application
